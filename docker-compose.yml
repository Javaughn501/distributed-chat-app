version: '3'

services:
  db:
    image: postgres:15
    restart: always
    ports:
      - '5432:5432'

    environment:
      POSTGRES_USER: chat
      POSTGRES_PASSWORD: chat
      POSTGRES_DB: chat

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: